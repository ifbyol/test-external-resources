# SQS Queue Notes

This is a sample SQS queue configuration. The queue URL is configured through an environment variable:

```
SQS_QUEUE_URL=${SQS_QUEUE_URL}
```

Make sure to set the environment variable before using the queue. 